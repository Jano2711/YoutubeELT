checks for yt_api:
  - missing_count("Video_ID") = 0
  - duplicate_count("Video_ID") = 0

  - likes_count_greater_than_video_views = 0:
      name: Check Like_Count Greater Than Video Views
      likes_count_greater_than_video_views query: |
        SELECT COUNT(*) AS likes_count_greater_than_video_views
        FROM yt_api
        WHERE "Likes_Count" > "Video_Views";